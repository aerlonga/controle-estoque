# Exemplo de configuração para rodar o BACKEND LOCALMENTE (npm run dev)
# Para rodar com Docker, configure o arquivo .env na RAIZ do projeto

PORT=3000

# URL do Banco de Dados
# Use 'localhost' se rodar o Node localmente e o Banco no Docker
DATABASE_URL="postgresql://usuario_postgres:senha_postgres@localhost:5432/nome_do_banco?schema=public"

# Autenticação JWT
JWT_SECRET="segredo_exemplo_dev"
JWT_EXPIRES_IN="7d"
